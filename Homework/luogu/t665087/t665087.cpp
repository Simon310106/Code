#include <bits/stdc++.h>
using namespace std;

int r[100005], c[100005];

int main(){
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++) {
            cin >> r[i] >> c[i];
        }
        
    }
    return 0;
}