#include <bits/stdc++.h>
using namespace std;

using PII = pair<int, int>;
const int N = 1e6 + 5, mod = 1e5 + 3;
vector<int> g[N];
int dist[N];
int ans[N];

int main(){
    int n, e;
    cin >> n >> e;
    for (int i = 1; i <= e; i++) {
        int x, y, w;
        cin >> x >> y >> w;
        
    }
    return 0;
}